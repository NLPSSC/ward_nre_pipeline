{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "[genalog_api] - Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}